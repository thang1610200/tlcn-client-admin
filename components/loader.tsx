"use client";

import { ClipLoader } from 'react-spinners';

const Loader = () => {
    return (
        <div>
            <ClipLoader size={30} color="#0284c7" />
        </div>
    );
}

export default Loader;